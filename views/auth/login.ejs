<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Usamos la variable pageTitle si existe, sino un título por defecto -->
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Comanda Digital' %></title>
    
    <!-- Bootstrap CSS desde un CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome para íconos (opcional pero recomendado) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    
    <style>
        /* Estilos personalizados para la página de login */
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-color: #f8f9fa;
        }
        .login-container {
            width: 100%;
            max-width: 420px;
            padding: 2rem;
            background-color: #ffffff;
            border-radius: 0.75rem;
            box-shadow: 0 1rem 3rem rgba(0, 0, 0, 0.1);
        }
        .login-header {
            margin-bottom: 2rem;
            text-align: center;
        }
        .login-header i {
            font-size: 3rem;
            color: #0d6efd;
        }
        .login-header h2 {
            margin-top: 1rem;
            font-weight: 700;
        }
    </style>
</head>
<body>

    <main class="login-container">
        <div class="login-header">
            <i class="fas fa-clipboard-list"></i>
            <h2>Comanda Digital</h2>
        </div>

        <!-- Incluimos el partial para mostrar mensajes de error/éxito -->
        <%- include('../partials/messages') %>

        <!-- Formulario de Login -->
        <form id="login-form" action="/login" method="POST">
            <div class="form-floating mb-3">
                <input type="email" class="form-control" id="email" name="email" placeholder="nombre@ejemplo.com" required autofocus>
                <label for="email">Correo Electrónico</label>
            </div>
            <div class="form-floating mb-3">
                <input type="password" class="form-control" id="password" name="password" placeholder="Contraseña" required>
                <label for="password">Contraseña</label>
            </div>
            <button type="submit" class="btn btn-primary w-100 btn-lg">Entrar</button>
        </form>

    </main>

    <!-- Bootstrap JS Bundle (incluye Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Script de depuración (opcional, puedes eliminarlo si ya todo funciona) -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.getElementById('login-form');
            if (form) {
                console.log("✅ Formulario de login encontrado. Method:", form.getAttribute('method'));
                form.addEventListener('submit', () => console.log("🚀 Evento 'submit' del formulario disparado!"));
            }
        });
    </script>
</body>
</html>